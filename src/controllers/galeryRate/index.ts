import type { NextFunction } from "express";

class GaleryRateController {
  async postGaleryRate(request: Request, response: Response, next: NextFunction) {
    try {
    } catch (error) {
      next(error);
    }
  }
}
