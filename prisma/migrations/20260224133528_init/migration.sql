-- CreateTable
CREATE TABLE "user" (
    "ID" UUID NOT NULL,
    "ROLE" TEXT NOT NULL DEFAULT 'USER',
    "EMAIL" TEXT NOT NULL,
    "PASSWORD" TEXT NOT NULL,
    "TOKEN" TEXT,
    "CREATEDAT" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "UPDATEDAT" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "user_pkey" PRIMARY KEY ("ID")
);

-- CreateTable
CREATE TABLE "checkin" (
    "ID" UUID NOT NULL,
    "NAME" TEXT NOT NULL,
    "CREATEDAT" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "UPDATEDAT" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "checkin_pkey" PRIMARY KEY ("ID")
);

-- CreateTable
CREATE TABLE "schema" (
    "ID" UUID NOT NULL,
    "TITLE" TEXT NOT NULL,
    "IDCHECKIN" UUID NOT NULL,
    "CREATEDAT" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "UPDATEDAT" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "schema_pkey" PRIMARY KEY ("ID")
);

-- CreateTable
CREATE TABLE "model" (
    "ID" UUID NOT NULL,
    "NAME" TEXT NOT NULL,
    "TITLE" TEXT NOT NULL,
    "QUESTION" TEXT NOT NULL,
    "ICONS" BOOLEAN NOT NULL DEFAULT false,
    "PHOTO" BOOLEAN NOT NULL DEFAULT false,
    "BONDPHOTO" BOOLEAN NOT NULL DEFAULT false,
    "LOCATION" BOOLEAN NOT NULL DEFAULT false,
    "VALUES" BOOLEAN NOT NULL DEFAULT false,
    "NOTES" BOOLEAN NOT NULL DEFAULT false,
    "IDSCHEMA" UUID NOT NULL,
    "CREATEDAT" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "UPDATEDAT" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "model_pkey" PRIMARY KEY ("ID")
);

-- CreateTable
CREATE TABLE "input" (
    "ID" UUID NOT NULL,
    "TYPE" TEXT NOT NULL,
    "NAME" TEXT NOT NULL,
    "LABEL" TEXT NOT NULL,
    "VALUE" TEXT NOT NULL,
    "MIN" INTEGER,
    "MAX" INTEGER,
    "PLACEHOLDER" TEXT,
    "REQUIRED" BOOLEAN NOT NULL DEFAULT false,
    "MULTIPLE" BOOLEAN NOT NULL DEFAULT false,
    "BOND" BOOLEAN NOT NULL DEFAULT false,
    "BONDTYPE" TEXT,
    "IDMODEL" UUID NOT NULL,
    "CREATEDAT" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "UPDATEDAT" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "input_pkey" PRIMARY KEY ("ID")
);

-- CreateTable
CREATE TABLE "enum" (
    "ID" UUID NOT NULL,
    "NAME" TEXT NOT NULL,
    "IDINPUT" UUID NOT NULL,
    "CREATEDAT" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "UPDATEDAT" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "enum_pkey" PRIMARY KEY ("ID")
);

-- CreateTable
CREATE TABLE "checkin_avaliar" (
    "ID" UUID NOT NULL,
    "IDCHECKIN" UUID NOT NULL,
    "NAME" TEXT NOT NULL,
    "VERSION" INTEGER NOT NULL,
    "JSON_CHECKIN" TEXT NOT NULL,
    "OBJECT_CHECKIN" TEXT NOT NULL,
    "ATIVE" BOOLEAN NOT NULL DEFAULT true,
    "CREATEDAT" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "UPDATEDAT" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "checkin_avaliar_pkey" PRIMARY KEY ("ID")
);

-- CreateTable
CREATE TABLE "avaliar" (
    "ID" UUID NOT NULL,
    "IDVERSIONCHECKIN" UUID NOT NULL,
    "RESULT" TEXT NOT NULL,
    "CREATEDAT" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "UPDATEDAT" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "avaliar_pkey" PRIMARY KEY ("ID")
);

-- CreateIndex
CREATE UNIQUE INDEX "user_EMAIL_key" ON "user"("EMAIL");

-- AddForeignKey
ALTER TABLE "schema" ADD CONSTRAINT "schema_IDCHECKIN_fkey" FOREIGN KEY ("IDCHECKIN") REFERENCES "checkin"("ID") ON DELETE RESTRICT ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "model" ADD CONSTRAINT "model_IDSCHEMA_fkey" FOREIGN KEY ("IDSCHEMA") REFERENCES "schema"("ID") ON DELETE RESTRICT ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "input" ADD CONSTRAINT "input_IDMODEL_fkey" FOREIGN KEY ("IDMODEL") REFERENCES "model"("ID") ON DELETE RESTRICT ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "enum" ADD CONSTRAINT "enum_IDINPUT_fkey" FOREIGN KEY ("IDINPUT") REFERENCES "input"("ID") ON DELETE RESTRICT ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "checkin_avaliar" ADD CONSTRAINT "checkin_avaliar_IDCHECKIN_fkey" FOREIGN KEY ("IDCHECKIN") REFERENCES "checkin"("ID") ON DELETE RESTRICT ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "avaliar" ADD CONSTRAINT "avaliar_IDVERSIONCHECKIN_fkey" FOREIGN KEY ("IDVERSIONCHECKIN") REFERENCES "checkin_avaliar"("ID") ON DELETE RESTRICT ON UPDATE CASCADE;
